name: Workflow
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: self-hosted
    strategy:
      fail-fast: false
      matrix:
        max-runs: 3
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run build
        run: npm run build