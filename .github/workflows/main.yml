name: Workflow
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run bulid
        run: npm run build
# 設定失敗的 job 自動重跑
retry:
  max-runs: 10  # 最多重試 3 次
  when:
    - failure